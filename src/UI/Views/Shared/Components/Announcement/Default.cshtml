@model AlbumDTO

@if (Model != null)
{
    <li>
        <br />
        <small><i><label title="New Arrivals!" style="color:red">New Arrivals!</label></i></small>
        <i><a id="NewArrivalsPanel" asp-area="" asp-controller="Store" asp-action="Details" asp-route-id="@Model.Id">@Model.Title</a></i>
    </li>
}